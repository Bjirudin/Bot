services:
  - type: worker
    name: chacha-ninggolan-bot
    runtime: node
    buildCommand: npm install
    startCommand: node bot.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
